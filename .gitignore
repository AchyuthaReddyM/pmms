node_modules/
pmms.db
pmms.db-journal
pmms.db-wal
pmms.db-shm
.DS_Store
*.log
.env
.env.local
.env.production
# OneDrive sync cruft
desktop.ini
Thumbs.db
# pkg build output — the .exe is generated, not committed
dist/
*.exe

# Licensing
# NEVER commit the private key — anyone with it can forge unlimited licenses.
tools/license_private.pem
# Runtime license file (sits next to the .exe on each install)
license.txt
# CSV ledger of licenses you've issued — sensitive but you decide whether to keep it.
tools/licenses_issued.csv
