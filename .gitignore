node_modules/
pmms.db
pmms.db-journal
pmms.db-wal
pmms.db-shm
.DS_Store
*.log
.env
.env.local
.env.production
# OneDrive sync cruft
desktop.ini
Thumbs.db
# pkg build output — the .exe is generated, not committed
dist/
*.exe

# Licensing — NEVER commit the private key (anyone with it can forge licenses).
lictool/license_private.pem
lictool/license_public.pem
lictool/licenses_issued.csv
# Runtime license file (sits next to the .exe on each install)
license.txt
# Old folder name — kept for safety in case any cache references it.
tools/license_private.pem
tools/license_public.pem
tools/licenses_issued.csv
