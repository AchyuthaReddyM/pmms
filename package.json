{
  "name": "pmms",
  "version": "1.0.0",
  "private": true,
  "description": "Preventive Maintenance & Breakdown Management System - Ways Automation",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "seed": "node -e \"require('./db').initAndSeed(true); console.log('Seeded.');\"",
    "reset": "node -e \"const fs=require('fs'); try{fs.unlinkSync('pmms.db')}catch(e){}; require('./db').initAndSeed(true); console.log('DB reset and seeded.');\""
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
